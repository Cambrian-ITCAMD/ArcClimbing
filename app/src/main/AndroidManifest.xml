<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ca.arcclimbing.forerunner">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/FirebaseUI">
        <activity android:name="ca.arcclimbing.forerunner.ProfileActivity" />
        <activity android:name="ca.arcclimbing.forerunner.EditRouteActivity" />
        <activity android:name="ca.arcclimbing.forerunner.RouteDetailsActivity" />
        <activity
            android:name="ca.arcclimbing.forerunner.MainActivity"
            android:theme="@style/FirebaseUI.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>